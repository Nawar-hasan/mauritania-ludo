# MAURITANIA LUDO V7 — Offline + Wager + Social + Web finalization

## ما تم تنفيذه

### 1) اللعب بدون إنترنت
- وضع Solo: اللاعب ضد كمبيوتر واحد.
- وضع Practice 4 Players: اللاعب ضد 3 لاعبين كمبيوتر.
- رقعة محلية فعلية، نرد، إخراج القطع، الالتقاط، الخانات الآمنة، المسار النهائي، الفوز، والأدوار الإضافية.
- لا يستخدم الخادم ولا يغيّر المحفظة أو الرهان أو XP أو الإحصائيات المتصلة.
- يمكن الدخول إليه حتى من شاشة Login عندما لا يوجد إنترنت.

### 2) الرهان — التدفق أصبح واضحاً
المسار في التطبيق:
Home → Wager Match → 2/4 Players → Rules → Choose Wager → Confirm → Matchmaking → Waiting Room → Match.

الباكند الحالي يحافظ على:
- minimum_wager / maximum_wager من الإعدادات.
- رفض WAGER بقيمة 0.
- فحص الرصيد قبل دخول البحث.
- حجز قيمة الرهان CASH → LOCKED داخل transaction.
- رد الرهان عند إلغاء الغرفة/مغادرة الانتظار.
- عند انتهاء المباراة: خصم LOCKED لكل لاعب، احتساب platform_fee_rate، وإيداع الجائزة في CASH للفائز.
- Ledger ومعاملات مالية منفصلة لكل حركة مالية.
- التسوية تعتمد على نتيجة محرك اللعب في الخادم، وليس نتيجة يرسلها العميل.

### 3) الدفع
البنية الحالية تدعم:
- MANUAL: تحويل + إيصال + مراجعة من الإدارة.
- MOOSYL: checkout + webhook + تحقق توقيع عند توفير مفاتيح المزود.
- CUSTOM: يحتاج Adapter حسب عقد API لمزود الدفع الرسمي.

مهم: لا يمكن ضمان أن أي API رسمي جديد يحتاج فقط URL/Key؛ إذا شكل الطلب/الرد/Webhook مختلفاً فيجب كتابة mapping صغير للمزود.

### 4) الغرف والدردشة
- PostgreSQL models للغرف، الأعضاء، والرسائل.
- إنشاء غرفة Text أو Voice.
- دخول/مغادرة الغرف.
- محادثة نصية حقيقية محفوظة في الخادم.
- حد أقصى للمشاركين.
- Voice Provider Adapter في الباكند.
- الصوت الحي نفسه يحتاج مزود صوت فعلي (API/SDK). لا يوجد نظام يستطيع نقل الصوت الحقيقي من دون مزود صوت أو WebRTC infrastructure.

متغيرات مزود الصوت:
- VOICE_PROVIDER
- VOICE_TOKEN_ENDPOINT
- VOICE_API_KEY

### 5) المستويات والمراحل والمتجر والصور
- المستويات والمراحل بقيت مرتبطة بالـCatalog/Admin والـBackend.
- Store/Inventory/Appearance بقيت مرتبطة بالخادم.
- رفع Avatar وإيصالات التحويل صار يعمل بطريقة bytes-compatible حتى يكون Flutter Web قابلاً للاختبار على iPhone.
- تحقق JPEG/PNG/WEBP في الخادم يعتمد أيضاً على magic bytes وليس MIME فقط.

### 6) Flutter Web
تمت إضافة:
`release_tools/build_web_from_railway.ps1`

ويستخدم نفس Backend في Railway.

## ما لا يمكن اعتباره مكتملًا بدون خدمة خارجية
- Live voice audio: يحتاج مزود الصوت المختار وSDK/API الخاص به.
- الدفع الإلكتروني الرسمي: يحتاج بيانات وعقد API المزود الفعلي.
- نشر iOS الحقيقي: يحتاج Apple Developer عندما يحين وقت النشر.

## وحدات أخرى ما زالت مستقلة عن هذه الدفعة
البطولات، الدعوات، الإنجازات، الدعم، واستعادة كلمة المرور ما زالت وحدات منفصلة في الكود الحالي. إذا كانت مطلوبة ضمن نطاق التسليم النهائي للعميل، يجب إكمالها قبل تسمية التطبيق 100% Final Commercial Release.
