'use client';
import { FormEvent,useState } from 'react'; import { Shell } from '@/components/shell'; import { api } from '@/lib/api';
export default function Wallets(){const [message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await api<any>('/admin/wallets/adjust',{method:'POST',body:JSON.stringify({userId:f.get('userId'),accountType:f.get('accountType'),amount:Number(f.get('amount')),reason:f.get('reason'),idempotencyKey:crypto.randomUUID()})});setMessage(`Recorded transaction ${r.id}`);e.currentTarget.reset()}catch(e:any){setMessage(e.message)}}return <Shell><h1>Wallet adjustment</h1><div className="card" style={{maxWidth:600}}><p>Every change creates an immutable transaction and ledger entry.</p><form onSubmit={submit}><input className="input" name="userId" placeholder="User UUID" required/><div className="space"/><select className="input" name="accountType"><option>CASH</option><option>BONUS</option><option>COINS</option><option>GEMS</option><option>LOCKED</option></select><div className="space"/><input className="input" name="amount" type="number" step="0.0001" placeholder="Positive credit / negative debit" required/><div className="space"/><input className="input" name="reason" placeholder="Reason" required/><div className="space"/><button className="btn">Record adjustment</button>{message&&<p>{message}</p>}</form></div></Shell>}
