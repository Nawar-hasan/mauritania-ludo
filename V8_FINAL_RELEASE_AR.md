# MAURITANIA LUDO — V8 Final Release Candidate

هذه الحزمة تغلق الوحدات التي بقيت واجهات مستقلة في الإصدارات السابقة، وتربطها بباكند وقاعدة بيانات فعلية، مع تحسينات أمان وحركة واجهة.

## مكتمل داخل التطبيق والباكند

- استعادة كلمة المرور: طلب رمز، تحقق، reset token، كلمة مرور جديدة، وإلغاء جلسات refresh القديمة.
- البطولات: إنشاء وإدارة من لوحة التحكم، تسجيل/انسحاب، bracket حقيقي، مباريات لاعبين اثنين، تقدم الفائز، وتسوية الجائزة.
- الإنجازات: تعريفات من لوحة الإدارة، تقدم حقيقي، claim مرة واحدة، ومكافآت عبر ledger.
- الدعوات: كود Referral فريد، تطبيق الكود مرة واحدة، مكافآت وسجل المدعوين.
- الدعم: تذاكر ورسائل وحالات وردود موظفي الدعم من لوحة الإدارة.
- KYC/العمر: رفع مستندات خاصة، مراجعة الإدارة، وحماية ميزات المال الحقيقي.
- الخصوصية ولوحة المتصدرين.
- تخزين خاص للإيصالات ووثائق الهوية وعدم نشرها كملفات static.
- حماية رفع الصور بفحص النوع الحقيقي وحجم الملف.
- تدوير Refresh Token، Rate limiting ذري في Redis، وقفل Redis آمن بالمقارنة والحذف الذري.
- منع كشف وجود الحساب في استعادة كلمة المرور.
- مهلة 8 ثوانٍ للاتصالات الخارجية بالبريد/الصوت/الدفع.
- حركات واجهة: Fade/Slide للصفحات، Reveal للعناصر، Press scale، Glow، حركات النرد، وHaptics قابلة للتحكم.
- الإصدار Mobile: `1.2.0+5`.

## الوضع المالي الآمن افتراضياً

الحزمة لا تشغّل المال الحقيقي تلقائياً. الإعدادات الافتراضية تبقي `real_money_enabled=false`، ولا تصبح عمليات الرهان/الإيداع/السحب الحقيقية متاحة إلا بعد قرار الإدارة وتجهيز المتطلبات النظامية والتحقق من الهوية. وضع الاختبار يمكن تشغيله منفصلاً من إعدادات الإدارة عند الحاجة لاختبار التدفقات بأرصدة تجريبية.

## تكاملات خارجية لا يمكن اختلاقها داخل الكود

الكود والـAdapters جاهزة، لكن التشغيل الفعلي يحتاج بيانات الخدمة الحقيقية:

- إرسال رمز استعادة كلمة المرور بالبريد: `RESEND_API_KEY` و `EMAIL_FROM` أو استبدال Adapter بمزود بريد آخر.
- الصوت الحي: `VOICE_TOKEN_ENDPOINT` و `VOICE_API_KEY` مع SDK/transport الخاص بمزود الصوت. الغرف والأعضاء والرسائل وقاعدة البيانات موجودة؛ نقل الصوت نفسه يحتاج مزود/بنية WebRTC فعلية.
- الدفع الآلي: بيانات API/Webhook الرسمية للمزود. الدفع اليدوي بالإيصال يبقى مساراً منفصلاً ومراجعاً من الإدارة.

لا تضع أي Secret في GitHub أو داخل التطبيق. استخدم Railway Variables / GitHub Secrets فقط.

## قبل التسليم

1. شغّل `release_tools/validate_v8_windows.ps1` محلياً.
2. راجع `git status` ثم commit/push.
3. تأكد من نجاح Prisma migration وHealthcheck في Railway.
4. تحقق أن Volume الخاص بالباكند مركب على `/app/uploads` قبل الاعتماد على الإيصالات ووثائق الهوية.
5. ابنِ APK release من Railway URL واختبر قائمة التسليم كاملة.
6. بعد نجاح نسخة Android فقط، ابنِ نسخة Web للآيفون.
